services:
  iroh-relay:
    image: n0computer/iroh-relay:latest
    restart: unless-stopped
    environment:
      - SERVICE_FQDN_IROHRELAY_3340
      - RUST_LOG=info
    ports:
      - "7842:7842/udp"
    command: ["--dev"]

volumes:
  certs:
